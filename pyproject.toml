[project]
name = "stl10-cnn-classifier"
version = "0.1.0"
description = "Course project for STL-10 image classification with CNNs."
readme = "README.md"
requires-python = "~=3.11.0"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "scikit-learn>=1.5",
    "torch>=2.3",
    "torchvision>=0.18",
    "tqdm>=4.66",
]

[tool.uv]
package = false
