seed: 42
device: cuda:0
model_type: baseline

data:
  root: ./dataset/STL10
  num_workers: 4
  val_ratio: 0.2
  batch_size: 64

train:
  epochs: 150
  lr: 0.001
  weight_decay: 1.0e-4

output_dir: ./outputs/baseline
